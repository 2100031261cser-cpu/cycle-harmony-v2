[build]
  command = "npm run build"
  publish = "dist"

# API proxy MUST come BEFORE the catch-all redirect
[[redirects]]
  from = "/api/*"
  to = "https://cycle-harmony-v2.onrender.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
